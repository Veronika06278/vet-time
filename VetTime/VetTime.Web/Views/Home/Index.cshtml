@model VetTime.Web.ViewModels.Home.HomeViewModel

@{
    ViewData["Title"] ="Find your veterinarian doctor";
}

<h2 class="mb-4">@ViewData["Title"]</h2>

@Html.ValidationSummary(true, "", new { @class = "text-danger" })

<div class="container">
    <div class="row align-items-center">
        <!-- 🧾 Form on the left -->
        <div class="col-md-6">
            <form method="post">
                <div class="form-floating mb-3">
                    <option value="">Name of the veterinarian</option>
                    <label asp-for="VetFullName">Full Name</label>
                    <input asp-for="VetFullName" class="form-control" placeholder="Full Name" />
                    
                </div>

                <div class="form-group mb-3">
                    <label asp-for="CityName" class="form-label">City</label>
                    <select asp-for="CityName" class="form-control custom-dropdown">
                        <option value="">Choose City</option>
                        @foreach (string city in Model.Cities)
                        {
                            <option value="@city">@city</option>
                        }
                    </select>
                </div>

                <div class="form-group mb-4">
                    <label asp-for="Specialization" class="form-label">Specialization</label>
                    <select asp-for="Specialization" class="form-control custom-dropdown">
                        <option value="">Choose what type your animal is</option>
                        @foreach (string specialization in Model.Specializations)
                        {
                            <option value="@specialization">@specialization</option>
                        }
                    </select>
                </div>

                <button type="submit" class="btn btn-primary">Search</button>
            </form>
        </div>

        <!-- 🖼 Image on the right -->
        <div class="col-md-6 d-flex justify-content-center">
            <img src="https://img.freepik.com/premium-photo/male-veterinarian-cartoon-flat-design-isolated_776894-118392.jpg?w=2000"
                 alt="Veterinarian Illustration"
                 class="img-fluid rounded"
                 style="max-height: 500px; width: 100%; object-fit: contain;" />
        </div>

    </div>
</div>

@Html.Partial("_ValidationScriptsPartial")





@* 
@model VetTime.Web.ViewModels.Home.HomeViewModel

@{
    ViewData["Title"] = "Choose a city";
}

<h2>@ViewData["Title"]</h2>

@Html.ValidationSummary(true, "", new { @class = "text-danger" })

<div class="col-md-4">
    <form method="post">

        <div class="form-floating mb-3">
            <input asp-for="@Model.VetFullName" class="form-control" autocomplete="" aria-required="true" placeholder="" />
            <label asp-for="@Model.VetFullName">Full Name</label>
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        </div>

        <div class="form-group mb-3">
            <select asp-for="@Model.CityName" class="form-control custom-dropdown">
                <option value="" selected>Choose City</option>
                @foreach (string city in Model.Cities)
                {
                    <option value="@city">@city</option>
                }
            </select>
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        </div>

        <div class="form-group mb-3">
            <select asp-for="@Model.Specialization" class="form-control custom-dropdown">
                <option value="" selected>Choose what type your animal is</option>
                @foreach (string specialization in Model.Specializations)
                {
                    <option value="@specialization">@specialization</option>
                }
            </select>
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        </div>

        <button type="submit" class="btn btn-primary">Search</button>

        
    </form>
    <div class="col-md-6 d-flex justify-content-center align-items-center">
        <img src="https://img.freepik.com/premium-photo/male-veterinarian-cartoon-flat-design-isolated_776894-118392.jpg?w=2000"
             alt="Veterinarian Illustration"
             class="img-fluid rounded shadow"
             style="max-height: 350px;" />
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
 *@